# CLI

Command-line tooling for Laravel Vite Translations.

## Commands

- `laravel-vite-translations init`
- `laravel-vite-translations codemod`
- `laravel-vite-translations doctor`

## Purpose

- bootstrap the plugin in an existing project
- migrate `__()` and `trans()` usage
- diagnose missing keys and configuration issues
